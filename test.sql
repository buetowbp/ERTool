CREATE TABLE Dept (
name varchar(15),
id int,
description varchar(500)
)
CREATE TABLE Topic (
topicID int,
name varchar(50),
description varchar(500)
)
CREATE TABLE Employee (
id int,
name varchar(30),
address varchar(50),
phone varchar(7),
startDate int
)
CREATE TABLE Research (
id int,
completion double,
salary double
)
